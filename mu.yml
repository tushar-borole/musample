---

environments:
  - name: acceptance
    cluster:
      maxSize: 2
  - name: production
    cluster:
      maxSize: 5

service:
  name: basic-example
  healthEndpoint: /
  port: 80
  pathPatterns:
    - /*
  pipeline:
    source:
      provider: S3
      repo: mu-pipeline/frontend/prod/latest.zip
